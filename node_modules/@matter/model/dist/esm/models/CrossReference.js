/**
 * @license
 * Copyright 2022-2025 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
const inspect = Symbol.for("nodejs.util.inspect.custom");
class CrossReference {
  document;
  section;
  static instances = {};
  constructor(definition) {
    if (typeof definition === "object") {
      this.document = definition.document;
      this.section = definition.section;
    } else {
      [this.document, this.section] = definition.split("\xA7");
    }
  }
  toString() {
    return `${this.document}\xA7${this.section}`;
  }
  static get(xref) {
    if (xref instanceof CrossReference) {
      return xref;
    }
    const key = typeof xref === "string" ? xref : `${xref.document}\xA7${xref.section}`;
    const canonical = this.instances[key];
    if (canonical) {
      return canonical;
    }
    return this.instances[key] = new CrossReference(xref);
  }
  [inspect](_depth, options, inspect2) {
    return inspect2 ? inspect2(this.toString(), options) : this.toString();
  }
}
export {
  CrossReference
};
//# sourceMappingURL=CrossReference.js.map
