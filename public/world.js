/**
 * Holds the world info and it's methods.
 * @returns {World}
 */
class World{
    constructor(){
    }
    
    /**
     * draws the world to the canvas
     * @returns {null}
     */
    draw(){
        fill(0, 0, 100)
        rect(0, 0, width, height)
    }
}